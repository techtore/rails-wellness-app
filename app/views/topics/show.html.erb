<h1><%=@topic.title%></h1>

<% @topic.entries.each do |entry|%>
    <p><%=entry.date.strftime('%a, %b %e, %Y')%></p>
    <p><%=entry.content%></p>
<%end%>

<%=link_to "Add a New Entry for this Topic", new_entry_path%>
