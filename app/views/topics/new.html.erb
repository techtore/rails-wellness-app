<h1>Create a new Topic for an area of wellness you'd like to focus on</h1>

<%=form_for @topic do |f|%>

   <% if @topic.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@topic.errors.count, "error") %> prohibited this Entry from being saved:</h2>
      <ul>
        <% @topic.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

    <%= f.label :title%>
    <%=f.text_field :title%>
   
    <%=f.submit "Create New Topic"%>
<%end%>